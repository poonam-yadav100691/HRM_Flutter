import 'package:flutter/material.dart';

import 'component/body.dart';

class PendingHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }

  //  Future<T> _onBackPressed<T>(BuildContext context, Widget page) {

}
